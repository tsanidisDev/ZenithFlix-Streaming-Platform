# ─── PostgreSQL ───────────────────────────────────────────────
POSTGRES_USER=zenithflix
POSTGRES_PASSWORD=changeme
POSTGRES_DB=zenithflix_db
POSTGRES_PORT=5432

# ─── Backend (NestJS) ─────────────────────────────────────────
DATABASE_URL=postgresql://zenithflix:changeme@localhost:5432/zenithflix_db
PORT=3001
JWT_SECRET=supersecretkey_changeme_in_production
JWT_EXPIRES_IN=7d
# Comma-separated list of allowed origins (add your Vercel URL in production)
CORS_ORIGIN=http://localhost:3000

# ─── Frontend (Next.js) ───────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:3001/api
