{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter frontend build",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
